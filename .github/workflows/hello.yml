name: Hello #ワークフロー名
on: [push] #プッシュされたときに実行
jobs: #ジョブの定義
  hello: #ジョブID
    runs-on: windows-latest #実行環境
    steps: #ステップの定義
      - run: echo "Hello, world!" #コマンドの実行
      - uses: actions/checkout@v4 #リポジトリのチェックアウト